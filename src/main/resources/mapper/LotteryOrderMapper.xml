<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.wanjune.minilottery.mapper.LotteryOrderMapper">

    <insert id="insert">
        INSERT INTO lottery_order (order_id, user_id, activity_id, award_id, award_name, status, expire_time)
        VALUES (#{orderId}, #{userId}, #{activityId}, #{awardId}, #{awardName}, #{status}, #{expireTime})
    </insert>

    <select id="queryByUserIdAndActivityId" resultType="LotteryOrder">
        SELECT * FROM lottery_order
        WHERE user_id = #{userId}
          AND activity_id = #{activityId}
        ORDER BY create_time DESC
    </select>

    <select id="queryByOrderId" resultType="LotteryOrder">
        SELECT * FROM lottery_order WHERE order_id = #{orderId}
    </select>

    <update id="updateStatus">
        UPDATE lottery_order SET status = #{status} WHERE order_id = #{orderId}
    </update>

    <select id="queryStalePendingOrders" resultType="LotteryOrder">
        SELECT *
        FROM lottery_order
        WHERE status = #{status}
          AND create_time &lt;= #{beforeTime}
          AND expire_time &gt; #{now}
        ORDER BY create_time ASC, id ASC
        LIMIT #{limit}
    </select>

</mapper>
